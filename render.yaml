services:
  - type: web
    name: tiktok-media-downloader
    env: node
    plan: free
    buildCommand: npm install && npx yt-dlp --update-to nightly
    startCommand: npm start
    healthCheckPath: /api/health
